// URL of the backend server
export const WS_BASE_URL = 'ws://localhost:8000';

// Scale factor for greenkeeper size
export const GREENKEEPER_SIZE_SCALE = 0.6;

// Scale factor for player size
export const PLAYER_SIZE_SCALE = 0.6;

// Scale factor for ball size
export const BALL_SIZE_SCALE = 0.6;

// Scale factor for flag size
export const FLAG_SIZE_SCALE = 0.6;
